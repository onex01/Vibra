# Vibra OS

**Версия ОС:** 0.3 "Cartridge"  
**Версия ядра:** 0.3.0  
**Архитектура:** x86_64 (планируется ARM64)  
**Язык:** Rust (ядро), C (портируемый софт)  
**Загрузчик:** Limine  
**Файловая система:** RamFS (в памяти)

## Описание

Vibra — это хобби-операционная система, разрабатываемая с нуля на Rust.  
Цель проекта — создать современную ОС с графическим интерфейсом, поддержкой приложений и эмуляторов ретро-консолей.

## Текущий статус

- [x] Загрузка через Limine (UEFI)
- [x] Вывод в последовательный порт (COM1)
- [x] Физический менеджер памяти (PMM)
- [x] Вывод на экран (framebuffer)
- [x] PS/2 клавиатура с поддержкой Shift
- [x] Графическая консоль с ASCII-арт логотипом
- [x] RamFS (файловая система в памяти)
- [x] Shell с базовыми командами

## Доступные команды shell

```bash
help             # показать справку
clear            # очистить экран
echo <text>      # вывести текст
quit             # остановить систему
version          # показать версию ОС
version kernel   # показать версию ядра
ls               # список файлов и директорий
touch <name>     # создать пустой файл
mkdir <name>     # создать директорию
cat <file>       # прочитать содержимое файла
rm <name>        # удалить файл или директорию
```

## Структура проекта

```
vibra/
── src/
│   ├── main.rs          # Точка входа и shell
│   ├── serial.rs        # Драйвер COM1
│   ├── keyboard.rs      # Драйвер PS/2 клавиатуры
│   ├── framebuffer.rs   # Графическая консоль
│   ├── memory/
│   │   ├── mod.rs
│   │   └── pmm.rs       # Физический менеджер памяти
│   └── fs/
│       ├── mod.rs
│       └── ramfs.rs     # Файловая система в памяти
── docs/
│   ├── README.md        # Этот файл
│   ├── architecture.md  # Архитектура системы
│   └── roadmap.md       # План развития
├── Makefile
├── limine.conf
├── linker.ld
└── Cargo.toml
```

## Версионирование

**Версия ОС** обновляется при добавлении новых модулей и приложений.  
**Версия ядра** обновляется при изменениях в ядре (память, драйверы, планировщик).

Пример: `Vibra OS v0.3 "Cartridge" | Kernel v0.3.0`

## Сборка и запуск

```bash
./build.sh  # Полная сборка и запуск
make run    # Быстрая сборка и запуск
```

## Лицензия

MIT License
