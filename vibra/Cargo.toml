[package]
name = "vibra"
version = "1.0.0"
edition = "2021"

[dependencies]
vibra-kernel = { path = "../kernel" }
limine = "0.6"
spin = "0.9.8"

[features]
default = ["serial-debug"]
serial-debug = []

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
